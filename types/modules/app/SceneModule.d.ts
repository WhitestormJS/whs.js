export class SceneModule {

  /**
   * @constructor Creates a scene module.
   * @param willSceneBeReplaced should be true only if you are going to overwrite scene dependency
   * even without the use of default one. Default is false.
   */
  constructor(willSceneBeReplaced?: boolean);

}
